"use client"

import {useRef} from "react";

export default function PdfList() {
    const reportTemplateRef = useRef(null);
    return (
        <div>
            <h1>PDF List</h1>
        </div>
    )
}